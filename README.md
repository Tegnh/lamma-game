# 🎮 منصة لمّة (Lamma) - لعبة "كلك"

**لمّة** هي منصة ألعاب اجتماعية تفاعلية (Multiplayer Web Game) تُلعب عبر الإنترنت في الوقت الفعلي. تعتمد اللعبة الحالية "كلك" على الخداع والمعلومات العامة، حيث يقوم اللاعبون باختراع إجابات مزيفة للأسئلة لمحاولة خداع أصدقائهم وحصد النقاط.

🔗 **رابط اللعبة :** [https://lamma-game.onrender.com](https://lamma-game.onrender.com)

---

## 🚀 المميزات الرئيسية (Features)
* **تزامن فوري (Real-time):** تجربة لعب سلسة بدون الحاجة لتحديث الصفحة بفضل تقنية WebSockets.
* **قاعدة بيانات ضخمة:** تحتوي اللعبة على أكثر من 360 سؤالاً مقسمة على فئات متنوعة (تاريخ، جغرافيا، علوم، الخ).
* **إدارة الغرف (Room Management):** نظام لوبي متكامل يسمح بإنشاء غرف خاصة، الانضمام عبر كود (6 رموز)، وتحديد إعدادات اللعبة من قبل المضيف.
* **تصميم متجاوب (Responsive):** واجهة مستخدم (UI) مصممة لتعمل بكفاءة على شاشات الهواتف المحمولة وأجهزة الكمبيوتر، مع دعم كامل للغة العربية (RTL).
* **مؤثرات صوتية:** نظام تفاعلي للأصوات (UI Feedback) يعزز من تجربة المستخدم أثناء اللعب.

---

## 🛠️ التقنيات المستخدمة (Tech Stack)
* **الواجهة الأمامية (Frontend):** HTML5, CSS3, Vanilla JavaScript.
* **الخادم (Backend):** Node.js, Express.js.
* **الاتصال الفوري:** Socket.io.
* **الاستضافة (Deployment):** Render (Web Service).

---

## الاستخدامات :
استخدمت موقع https://mixkit.co/free-sound-effects للتحميل الاصوات 

## 🧩 أبرز التحديات البرمجية والحلول (Challenges & Solutions)

أثناء مرحلة التطوير، واجه المشروع عدة تحديات تقنية تم التعامل معها برمجياً لضمان استقرار الخادم:

### 1. مشكلة المصفوفات المتداخلة في قاعدة البيانات (Nested JSON Arrays)
* **المشكلة:** عند توسيع قاعدة الأسئلة، تم إدراج مصفوفات فرعية بالخطأ داخل المصفوفة الرئيسية في ملف `questions.json`. هذا أدى إلى وجود أكثر من 180 سؤالاً غير قابل للوصول، وكان سيؤدي إلى انهيار الخادم (Crash) إذا حاولت خوارزمية السحب العشوائي الوصول إلى كائن غير محدد (`undefined`).
* **الحل:** تمت كتابة سكربت `Node.js` مخصص لقراءة الملف، واستخدام دالة التسوية `.flat(Infinity)` لفك جميع المصفوفات المتداخلة آلياً، ثم إعادة حفظ الملف بتنسيق مسطح ونظيف لتجنب أي أخطاء يدوية.

### 2. التوجيه الثابت للملفات ومشاكل (CORS) في بيئة الإنتاج
* **المشكلة:** عند التجهيز للرفع على خوادم `Render`، ظهرت مشاكل تتعلق بعدم قدرة الواجهة على قراءة مسارات الأصوات والصور بسبب قيود التوجيه، بالإضافة إلى مشاكل المنافذ الثابتة (`Port 3000`).
* **الحل:** تم تعديل `server.js` ليكون المنفذ ديناميكياً `process.env.PORT`. كما تم استخدام `express.static` لخدمة مجلدات `public` بشكل صحيح. وفي جهة العميل (`socket-client.js`)، تم جعل رابط الاتصال ديناميكياً `io()` ليتصل تلقائياً بنطاق الاستضافة الفعلي دون كتابة الروابط يدوياً.

### 3. أخطاء التزامن وحالات السباق (Race Conditions)
* **المشكلة:** في الألعاب التي تعتمد على الوقت الفعلي، يمكن أن تحدث مشاكل التزامن إذا قام اللاعبون بالتصويت في نفس اللحظة أو إذا انتهى المؤقت أثناء وصول البيانات.
* **الحل:** تم استخدام بنية الخادم الموثوق (Server-Authoritative Architecture). الخادم هو من يقرر صحة الإجابات، وتم إضافة متغيرات حالة (Flags) مثل `isTransitioning` لمنع تكرار تنفيذ دوال الانتقال بين مراحل اللعبة، واستخدام بنية `Map()` لتسجيل الأصوات وضمان عدم قدرة اللاعب على التصويت المزدوج.

### 4. حماية منطق اللعبة
* **المشكلة:** إمكانية بدء اللعبة بعدد لاعبين غير كافٍ مما يفسد منطق مرحلة "التصويت".
* **الحل:** تمت إضافة شروط تحقق (Validation) تمنع المضيف من بدء اللعبة ما لم يكتمل الحد الأدنى المطلوب وهو 3 لاعبين، مما يضمن سير أطوار اللعبة بشكل سليم.

---

## ⚙️ التشغيل المحلي (How to Run Locally)

لنسخ المشروع وتشغيله على جهازك:

1. قم باستنساخ المستودع:
   ```bash
   git clone [https://github.com/Tegnh/lamma-game.git](https://github.com/Tegnh/lamma-game.git)



# الاوامر المستخدمة في المشروع :
# 🎮 منصة لمّة (Lamma) - لعبة خَرّاط (Kharrat)

منصة ألعاب ويب اجتماعية تفاعلية (Real-time Multiplayer) تعتمد على الخداع والتصويت، مبنية باستخدام Node.js و Socket.io.

---

## 💻 1. أوامر التشغيل الأساسية (Terminal Commands)

### تشغيل اللعبة محلياً (Local Testing)
يجب التأكد أولاً من الدخول لمجلد المشروع الصحيح:
```bash
cd game-platform
node backend/server.js

#دورة رفع التحديثات (Git Workflow)

git add .
git commit -m "اكتب وصف التعديل هنا"
git push

#حل مشكلة تعارض الرفع (Git Pull)

git pull
git push

#🤖 2. مكتبة الموجهات للذكاء الاصطناعي (Claude Prompts Library)

🧠 أ. موجه "تهيئة السياق" (يُرسل في بداية أي محادثة جديدة)

أنت الآن تعمل كمهندس برمجيات رئيسي (Senior Full-Stack Engineer). أريدك أن تفهم وتستوعب البنية المعمارية لمشروعي الحالي لكي تساعدني في تطويره.
- المشروع: منصة "لمّة" بداخلها لعبة "خرّاط" (لعبة خداع وتصويت Multiplayer).
- التقنيات: Node.js, Express, Socket.io, Vanilla JS.
- البنية: Server-Authoritative (الخادم يدير المنطق في KalakGame.js و RoomManager.js والعميل يعرض الواجهات فقط).
- يرجى الحفاظ على دعم اللغة العربية (RTL) وعدم كسر منطق الخادم عند طلب أي تعديلات قادمة. أنا جاهز لإعطائك الطلبات.

#🔗 ب. موجه "إضافة رابط الدعوة المباشر"
أريد إضافة ميزة "رابط الدعوة المباشر" في `ui-kalak.js`.
عند فتح الصفحة، افحص الرابط (URLSearchParams) بحثاً عن متغير `code`. إذا وُجد، ضعه تلقائياً في حقل الإدخال وانقل التركيز (Focus) لحقل الاسم.
وفي شاشة اللوبي، اجعل زر "نسخ الكود" ينسخ الرابط كاملاً (رابط الموقع + ?code=الكود) لتسهيل دعوة الأصدقاء.

#👥 ج. موجه "تطوير وضع الفرق" (Teams Mode)

أريد إضافة "وضع الفرق" (Teams Mode).
1. الواجهة (`kalak.html` / `ui-kalak.js`): خيار للمضيف لتحديد عدد الفرق (2-4)، وأزرار للاعبين لاختيار فريقهم في اللوبي.
2. الخادم (`RoomManager.js`): إضافة `teamId` لكل لاعب وإدارة حدث اختيار الفريق.
3. منطق اللعبة (`KalakGame.js`): شرط بدء اللعبة يتطلب لاعبين في فريقين مختلفين على الأقل. وفي مرحلة الكشف، تُجمع نقاط الفرد وتُضاف أيضاً إلى `teamScores`.
4. النتائج: عرض الفريق الفائز وترتيب اللاعبين. يرجى التعديل مباشرة (Edit automatically).

#🛠️ د. موجه "إصلاح خطأ شاشة النتائج" (Debugging)

يوجد خطأ (Silent Error) يوقف عمل شاشة النتائج عند الضغط على زر "عرض النتائج النهائية".
يرجى مراجعة دالة `renderGameEnd` والـ Event Listener الخاص بها. أضف شروط حماية (Guard clauses / Null checks) للتأكد من أن الكود لا يتعطل إذا لم يجد عنصر HTML معين (مثل أيقونة التاج) أو إذا كانت اللعبة في الوضع الفردي (لا يوجد teamScores). أعطني الكود المصلح فوراً.

#🎨 هـ. موجه "بناء وتعديل واجهة V3.0 التجريبية"

يرجى إجراء تعديلات على نموذج `kharraat-v3.html` التجريبي:
1. صحح النصوص: المنصة اسمها "لمّة"، والبطاقة في المنتصف للعبة "خَرّاط".
2. التنسيق: استخدم Flexbox لضبط شريط التنقل السفلي (`justify-content: space-around`) وتوسيط العناصر.
3. الأداء: الواجهة بطيئة. قلل استخدام `backdrop-filter: blur`، وأضف `will-change: transform;` للعناصر التي تتحرك 3D لتحسين أداء الرسوميات.
4. فعل زر "العب الآن" لينقل لصفحة `kalak.html`.
